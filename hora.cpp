#include <iostream>
#include <conio.h>
using namespace std;
main()
{
	int h, m, s,ss,hh,mm;
	cout<<"Ingrese las horas: ";
	cin>>h;
	cout<<"Ingrese los minutos: ";
	cin>>m;
	cout<<"Ingrese los segundos: ";
	cin>>s;
	
		if(s<=58)
	
		if(m<=58)

		if(h<=23)
	
   cout<<(h+1)<<"h"<<" "<<(m+1)<<"m"<<" "<<(s+1)<<"s";
   
   	if (m==59)
		
	if (s==59)
	
	if (h==24)
	
	cout<<"00h 00m 00s";
   getch();
}
